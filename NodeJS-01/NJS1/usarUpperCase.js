const upperCase = require('upper-case').upperCase; //importa una funcion de upperCase

const texto = 'hola mundo desde node.js'; // en la constante texto se guarda el mensaje entre comillas
console.log(upperCase(texto)); // se usa e upper case en texto